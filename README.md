# EnvSync

Local-first, deterministic environment variable consistency checks for JavaScript and TypeScript projects.

> Repository bootstrap. The v0.1.0 implementation is developed on `feat/envsync-v0.1.0`.
